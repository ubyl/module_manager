<form method="post" action="{{ path('app_form_pai_braden_delete', {'id': braden.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ braden.id) }}">
    <button class="btn">Elimina</button>
</form>
