<form method="post" action="{{ path('app_controller_pai_bisogni_delete', {'id': bisogni.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bisogni.id) }}">
    <button class="btn">Delete</button>
</form>
