<form method="post" action="{{ path('app_parere_mmg_delete', {'id': parere_mmg.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ parere_mmg.id) }}">
    <button class="btn">Cancella</button>
</form>
